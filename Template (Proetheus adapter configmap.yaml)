apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "blockchain-gateway.fullname" . }}-promadapter
  labels:
    app: {{ include "blockchain-gateway.name" . }}
data:
  custom-metrics-config.yaml: |
    rules:
{{ toYaml .Values.prometheusAdapter.rules | indent 6 }}
