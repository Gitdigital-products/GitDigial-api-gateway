{
  "name": "@yourorg/blockchain-gateway-sdk",
  "version": "0.1.0",
  "description": "TypeScript SDK for Blockchain API Gateway — simple client for RPC proxy, tx submit, webhooks.",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "files": [
    "dist/**/*"
  ],
  "scripts": {
    "build": "tsc -p tsconfig.build.json",
    "lint": "eslint src --ext .ts",
    "test": "vitest",
    "prepublishOnly": "npm run build"
  },
  "keywords": ["blockchain", "gateway", "sdk", "evm"],
  "author": "YourOrg",
  "license": "MIT",
  "dependencies": {
    "cross-fetch": "^3.1.5"
  },
  "devDependencies": {
    "typescript": "^5.2.0",
    "vitest": "^0.34.0",
    "@types/node": "^18.0.0",
    "eslint": "^8.0.0"
  }
}
